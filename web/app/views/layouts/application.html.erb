<!DOCTYPE html>
<html>
<head>
  <title>Excel</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	<link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">
	<link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css" rel="stylesheet">
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	<script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
		<div class="navbar-header">
			<a class="navbar-brand" href="/">Twilio + Excel</a>
		</div>
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <a href="/call_histories">通話履歴</a>
        </li>
        <li>
          <a href="/user_apps">利用者一覧</a>
        </li>
      </ul>
<% if logged_in? %>
		<p class="navbar-text navbar-right">Signed in as <a href="<%= "/users/#{current_user[:id]}" %>" class="navbar-link"><%= current_user[:username]%></a></p>
<% end %>
		</div>
  </div>
</nav>
<% if logged_in? %>
<%= link_to "Logout", logout_url, :method => :delete %>
<% else %>
<%= link_to "Login", login_url %>
<% end %>
<div class="main">
<%= yield %>
</div>
</body>
</html>
